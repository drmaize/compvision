Readme for 1_imageJFiles

Description:

- Modified PairWiseStitching.java, PhaseCorelation.java (now ignoring search in z direction)
- Added GenericDialog1 (without the graphical dependencies of GenericDialog) 
- Added GenericDialogPlus to stitching plugin (this is different from Fiji's GenericDialogPlus, inherits from GenericDialog1 instead of GenericDialog)


Usage:
	- Copy Stitching jar into "plugins" folder and the rest into "jars" folder of the Fiji installation directory


Notes:
	- jars were compiled using ij-1.48k.jar.